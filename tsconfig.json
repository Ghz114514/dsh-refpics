{
  // Solution file: one program per side (host half vs browser half). The
  // shared src/core modules compile in both programs; host-only SDK merges
  // (sessions etc.) and browser-only DOM/JSX cannot share one ts.Program.
  "files": [],
  "references": [
    { "path": "./tsconfig.host.json" },
    { "path": "./tsconfig.client.json" }
  ]
}
